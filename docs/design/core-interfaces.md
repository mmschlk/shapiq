# Core interfaces

Superseded by ADR 0015 (explanations are games). The current vocabulary is
CONTEXT.md (binding glossary); the design record with evidence is
`docs/plans/issue-14-value-functions-api.md`. The sketch that used to live
here described the pre-0015 interface (explanation arrays, `sample()`,
approximation states) and was retired rather than half-trusted.
